<?xml version="1.0" encoding="utf-8"?>
<templates>
    <t t-name="cyllo_purchase_digitization.ListView.Buttons">
        <div t-if="props.showButtons"
             class="o_list_buttons d-flex gap-1 d-empty-none align-items-baseline"
             role="toolbar"
             aria-label="Main actions">
            <t t-if="model.root.editedRecord">
                <button type="button"
                        class="btn btn-primary o_list_button_save"
                        data-hotkey="s"
                        t-on-click.stop="onClickSave">
                    Save
                </button>
                <button type="button"
                        class="btn btn-secondary o_list_button_discard"
                        data-hotkey="j"
                        t-on-click.stop="onClickDiscard"
                        t-on-mousedown="onMouseDownDiscard">
                    Discard
                </button>
            </t>
            <input type="file"
                   t-ref="uploadFileInput"
                   class="o_input_file o_hidden"
                   t-on-change.stop="onFileInputChange"/>
            <button type="button"
                    t-attf-class="btn btn-secondary"
                    t-on-click.stop.prevent="() => this.uploadFileInputRef.el.click()">
                Upload
            </button>
        </div>
    </t>
</templates>