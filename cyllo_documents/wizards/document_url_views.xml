<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Define a form view for the document.url model -->
    <record id="view_document_url_form" model="ir.ui.view">
        <field name="name">view.document.url.form</field>
        <field name="model">document.url</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <group>
                        <field name="url"/>
                        <field name="name" invisible="1"/>
                    </group>
                    <group>
                        <field name="preview" invisible="1"/>
                        <field name="workspace_id"/>
                    </group>
                </group>
                <footer>
                    <button class="btn btn-primary" string="UPLOAD" name="action_add_url" type="object"/>
                    <button class="btn" string="DISCARD" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
    <!-- Define an action to open the "Document URL" window -->
    <record id="action_view_document_url" model="ir.actions.act_window">
        <field name="name">Document URL</field>
        <field name="res_model">document.url</field>
        <field name="view_mode">form</field>
    </record>
</odoo>
