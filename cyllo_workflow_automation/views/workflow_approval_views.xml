<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- ================================================================
             APPROVAL REQUEST — List / Tree view
        ================================================================ -->
        <record id="view_workflow_approval_request_tree" model="ir.ui.view">
            <field name="name">workflow.approval.request.tree</field>
            <field name="model">workflow.approval.request</field>
            <field name="arch" type="xml">
                <tree string="Approval Requests"
                      decoration-info="state == 'pending'"
                      decoration-success="state == 'approved'"
                      decoration-danger="state == 'rejected'"
                      decoration-muted="state in ('expired', 'cancelled')">
                    <field name="display_name"/>
                    <field name="workflow_id"/>
                    <field name="approver_name"/>
                    <field name="state" widget="badge"/>
                    <field name="create_date"/>
                    <field name="expiration"/>
                    <field name="responded_at"/>
                </tree>
            </field>
        </record>

        <!-- Form view -->
        <record id="view_workflow_approval_request_form" model="ir.ui.view">
            <field name="name">workflow.approval.request.form</field>
            <field name="model">workflow.approval.request</field>
            <field name="arch" type="xml">
                <form string="Approval Request">
                    <header>
                        <button name="action_approve"
                                string="Approve"
                                type="object"
                                class="btn-success"
                                invisible="state != 'pending'"/>
                        <button name="action_reject"
                                string="Reject"
                                type="object"
                                class="btn-danger"
                                invisible="state != 'pending'"/>
                        <button name="action_cancel"
                                string="Cancel"
                                type="object"
                                invisible="state != 'pending'"/>
                        <field name="state" widget="statusbar"
                               statusbar_visible="pending,approved,rejected,expired,cancelled"/>
                    </header>
                    <sheet>
                        <group>
                            <group string="Workflow">
                                <field name="workflow_id"/>
                                <field name="node_id"/>
                                <field name="execution_id"/>
                            </group>
                            <group string="Approver">
                                <field name="approver_id"/>
                                <field name="approver_name"/>
                                <field name="approver_email"/>
                            </group>
                        </group>
                        <group>
                            <group string="Timing">
                                <field name="create_date" readonly="1"/>
                                <field name="expiration"/>
                                <field name="responded_at" readonly="1"/>
                            </group>
                            <group string="Record">
                                <field name="res_model"/>
                                <field name="res_id"/>
                                <field name="approver_ip" readonly="1"/>
                            </group>
                        </group>
                        <group string="Token">
                            <field name="token" readonly="1"/>
                            <field name="token_used" readonly="1"/>
                        </group>
                        <group string="Response">
                            <field name="response_comment" widget="html"/>
                        </group>
                        <notebook>
                            <page string="Audit Log">
                                <field name="log_ids" readonly="1">
                                    <tree>
                                        <field name="create_date"/>
                                        <field name="event" widget="badge"/>
                                        <field name="user_id"/>
                                        <field name="comment"/>
                                    </tree>
                                </field>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Search view -->
        <record id="view_workflow_approval_request_search" model="ir.ui.view">
            <field name="name">workflow.approval.request.search</field>
            <field name="model">workflow.approval.request</field>
            <field name="arch" type="xml">
                <search string="Approval Requests">
                    <field name="display_name"/>
                    <field name="workflow_id"/>
                    <field name="approver_name"/>
                    <filter string="Pending" name="pending" domain="[('state','=','pending')]"/>
                    <filter string="Approved" name="approved" domain="[('state','=','approved')]"/>
                    <filter string="Rejected" name="rejected" domain="[('state','=','rejected')]"/>
                    <filter string="Expired" name="expired" domain="[('state','=','expired')]"/>
                    <filter string="Cancelled" name="cancelled" domain="[('state','=','cancelled')]"/>
                    <separator/>
                    <group expand="0" string="Group By">
                        <filter string="State" name="group_state" context="{'group_by': 'state'}"/>
                        <filter string="Workflow" name="group_workflow" context="{'group_by': 'workflow_id'}"/>
                    </group>
                </search>
            </field>
        </record>

        <!-- Action -->
        <record id="action_workflow_approval_requests" model="ir.actions.act_window">
            <field name="name">Approval Requests</field>
            <field name="res_model">workflow.approval.request</field>
            <field name="view_mode">tree,form</field>
            <field name="view_id" ref="view_workflow_approval_request_tree"/>
            <field name="context">{'search_default_pending': 1}</field>
        </record>

        <!-- Menu items under Workflow Automation -->
        <menuitem id="menu_workflow_approvals"
                  name="Approvals"
                  parent="menu_workflow"
                  sequence="25"
                  action="action_workflow_approval_requests"/>

    </data>
</odoo>
