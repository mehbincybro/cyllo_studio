<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="approval_portal_template" name="Workflow Approval">
        <t t-call="web.layout">
            <t t-set="head">
                <style>
                    body {
                        background-color: #f8f9fa;
                        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
                    }
                    .approval-container {
                        max-width: 600px;
                        margin: 40px auto;
                        background: #ffffff;
                        border-radius: 8px;
                        box-shadow: 0 4px 6px rgba(0,0,0,0.1);
                        overflow: hidden;
                    }
                    .approval-header {
                        background: #875a7b;
                        color: white;
                        padding: 20px;
                        text-align: center;
                    }
                    .approval-header h2 {
                        margin: 0;
                        font-size: 24px;
                    }
                    .approval-body {
                        padding: 30px;
                    }
                    .btn-approve {
                        background-color: #28a745;
                        color: white;
                        border: none;
                        padding: 10px 20px;
                        border-radius: 4px;
                        cursor: pointer;
                        font-size: 16px;
                    }
                    .btn-reject {
                        background-color: #dc3545;
                        color: white;
                        border: none;
                        padding: 10px 20px;
                        border-radius: 4px;
                        cursor: pointer;
                        font-size: 16px;
                    }
                    .btn:hover {
                        opacity: 0.9;
                    }
                    .status-badge {
                        display: inline-block;
                        padding: 5px 10px;
                        border-radius: 4px;
                        font-weight: bold;
                        text-transform: uppercase;
                        font-size: 14px;
                    }
                    .status-approved { background: #d4edda; color: #155724; }
                    .status-rejected { background: #f8d7da; color: #721c24; }
                    .status-expired { background: #fff3cd; color: #856404; }
                </style>
            </t>
            
            <div class="approval-container">
                <div class="approval-header">
                    <h2><t t-esc="workflow_name"/></h2>
                </div>
                
                <div class="approval-body">
                    <t t-if="error">
                        <div class="alert alert-danger">
                            <t t-esc="error"/>
                        </div>
                    </t>
                    <t t-elif="already_decided">
                        <div class="text-center">
                            <h3 class="mb-4">This request has already been processed.</h3>
                            <div t-attf-class="status-badge status-{{state}}">
                                <t t-esc="state"/>
                            </div>
                        </div>
                    </t>
                    <t t-elif="expired">
                        <div class="text-center">
                            <h3 class="mb-4">This request has expired.</h3>
                            <div class="status-badge status-expired">Expired</div>
                        </div>
                    </t>
                    <t t-elif="success">
                        <div class="text-center">
                            <h3 class="mb-4">Thank you! Your decision has been recorded.</h3>
                            <div t-attf-class="status-badge status-{{state}}">
                                <t t-esc="decision"/>
                            </div>
                        </div>
                    </t>
                    <t t-else="">
                        <div class="text-center mb-4">
                            <h4>Hello <t t-esc="approver_name"/>,</h4>
                            <p>Your approval is required for this request.</p>
                        </div>
                        
                        <form method="post" class="mt-4">
                            <div class="mb-4">
                                <label for="comment" class="form-label text-muted">Optional Comment:</label>
                                <textarea name="comment" id="comment" class="form-control" rows="3"></textarea>
                            </div>
                            
                            <div class="d-flex justify-content-center gap-3">
                                <button type="submit" t-attf-formaction="/workflow/approval/{{token}}/approve" class="btn btn-approve">
                                    Approve
                                </button>
                                <button type="submit" t-attf-formaction="/workflow/approval/{{token}}/reject" class="btn btn-reject">
                                    Reject
                                </button>
                            </div>
                        </form>
                    </t>
                </div>
            </div>
        </t>
    </template>
</odoo>
