<templates>
     <t t-name="RealTimeInput">
        <input type="text"
               class="o_input"
               t-att-value="props.startEmpty ? '' : props.value"
               t-on-input="(ev) => props.update(ev.target.value)"
        />
    </t>
</templates>