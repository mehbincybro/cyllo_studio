<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!--Sale Order Line View Inherit-->
    <record id="sale_order_line_view_form_readonly" model="ir.ui.view">
        <field name="name">sale.order.line.form.readonly</field>
        <field name="model">sale.order.line</field>
        <field name="inherit_id" ref="sale.sale_order_line_view_form_readonly"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='company_id']" position="after">
                <field name="price_subtotal_latest" invisible="0"/>
            </xpath>
        </field>
    </record>
    <!--Sale Order View Inherit-->
    <record id="view_order_form" model="ir.ui.view">
        <field name="name">sale.order.form.inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='company_id']" position="after">
                <field name="is_paid" invisible="1"/>
                <field name="payment_state" invisible="1"/>
                <field name="amount_untaxed_latest" invisible="1"/>
            </xpath>
        </field>
    </record>
</odoo>