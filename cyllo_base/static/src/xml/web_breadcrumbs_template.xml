<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">
    <t t-inherit="web.Breadcrumbs" t-inherit-mode="extension" owl="1">
        <xpath expr="//ol[hasclass('breadcrumb')]" position="replace">
            <ol class="breadcrumb flex-nowrap text-nowrap small lh-sm">
                <li t-if="collapsedBreadcrumbs.length"
                    class="breadcrumb-item d-inline-flex min-w-0">
                    <Dropdown togglerClass="'btn btn-light btn-sm px-1 p-0'">
                        <t t-set-slot="toggler">
                            <i class="fa fa-ellipsis-h"/>
                        </t>
                        <t t-foreach="collapsedBreadcrumbs" t-as="breadcrumb"
                           t-key="breadcrumb.jsId">
                            <DropdownItem
                                    onSelected="() => this.onBreadcrumbClicked(breadcrumb.jsId)">
                                <t t-call="web.Breadcrumb.Name"/>
                            </DropdownItem>
                        </t>
                    </Dropdown>
                </li>
                <t t-foreach="visiblePathBreadcrumbs" t-as="breadcrumb"
                   t-key="breadcrumb.jsId">
                    <li class="breadcrumb-item d-inline-flex min-w-0"
                        t-att-class="{ o_back_button: breadcrumb_last }"
                        t-att-data-hotkey="breadcrumb_last and 'b'"
                        t-on-click.prevent="() => this.onBreadcrumbClicked(breadcrumb.jsId)">
                        <a href="#" class="fw-bold text-truncate"
                           t-att-data-tooltip="'Back to &quot;' + breadcrumb.name + '&quot;'">
                            <t t-call="web.Breadcrumb.Name"/>
                            <img class="breadcrumb_image"
                                 src="/cyllo_base/static/src/img/cy_statusbar_arrow.svg"
                                 alt=""/>
                        </a>
                    </li>
                </t>
            </ol>
        </xpath>
    </t>
</templates>