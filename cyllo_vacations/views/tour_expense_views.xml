<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <record id="tour_expense_view_tree" model="ir.ui.view">
        <field name="name">tour.expense.view.tree</field>
        <field name="model">tour.expense</field>
        <field name="arch" type="xml">
            <tree string="Tour Expenses" editable="bottom">
                <field name="date"/>
                <field name="booking_id"/>
                <field name="name"/>
                <field name="expense_type"/>
                <field name="amount" widget="monetary" sum="total"/>
                <field name="currency_id" column_invisible="1"/>
                <field name="payment_method"/>
                <field name="paid_by" widget="many2one_avatar_user"/>
                <field name="state"/>
            </tree>
        </field>
    </record>
    
    <record id="tour_expense_view_form" model="ir.ui.view">
        <field name="name">tour.expense.view.form</field>
        <field name="model">tour.expense</field>
        <field name="arch" type="xml">
            <form string="Tour Expense">
                <header>
                    <button name="action_approve" type="object" string="Approve" class="oe_highlight" invisible="state != 'draft'"/>
                    <button name="action_reject" type="object" string="Reject" invisible="state != 'draft'"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="booking_id"/>
                            <field name="date"/>
                            <field name="expense_type"/>
                        </group>
                        <group>
                            <field name="currency_id"/>
                            <field name="amount" widget="monetary"/>
                            <field name="payment_method"/>
                            <field name="paid_by"/>
                        </group>
                    </group>
                    <group>
                        <field name="name" placeholder="Expense description..."/>
                        <field name="reference"/>
                        <field name="invoice_reference"/>
                    </group>
                    <notebook>
                        <page string="Notes">
                            <field name="notes" placeholder="Additional notes..."/>
                        </page>
                        <page string="Attachments">
                            <field name="attachment_ids" widget="many2many_binary"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>
    
    <record id="action_tour_expense" model="ir.actions.act_window">
        <field name="name">Tour Expenses</field>
        <field name="res_model">tour.expense</field>
        <field name="view_mode">list,form</field>
    </record>
    
</odoo>

